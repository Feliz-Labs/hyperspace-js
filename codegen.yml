overwrite: true
schema: http://localhost:8080/graphql
documents: "./**/*.graphql"
generates:
  src/generatedSdk.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
