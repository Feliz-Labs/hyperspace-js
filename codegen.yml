overwrite: true
schema: http://localhost:8080/graphql
documents: "./**/*.graphql"
generates:
  src/sdk.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
